<!-- ======= Header ======= -->
<header id="header" class="fixed-top d-flex align-items-center">
    <div class="container d-flex align-items-center justify-content-between">

{#        <h1 class="logo"><a href="index.html">BelaJardins</a></h1>#}
        <!-- Uncomment below if you prefer to use an image logo -->
        <a href="#l" class="logo"><img src="{{ asset('build/images/bela_logo.png') }}" alt="" class="img-fluid"></a>

        <nav id="navbar" class="navbar">
            <ul>
                <li><a class="nav-link scrollto active" href="#hero">Accueil</a></li>
                {% if about is not null %}
                    <li><a class="nav-link scrollto" href="#about">{{ about.title }}</a></li>
                {% endif %}

                {% if service is not null %}
                    <li><a class="nav-link scrollto" href="#services">{{ service.title }}</a></li>
                {% endif %}

                {% if portfolio is not null %}
                    <li><a class="nav-link scrollto " href="#portfolio">{{ portfolio.title }}</a></li>
                {% endif %}

                {% if contact is not null %}
                    <li><a class="nav-link scrollto" href="#contact">Contact</a></li>
                {% endif %}

                {% if is_granted("ROLE_ADMIN") %}
                <li><a class="getstarted scrollto" href="{{ path('backoffice_main') }}">Administration</a></li>
                {% endif %}
            </ul>
            <i class="bi bi-list mobile-nav-toggle"></i>
        </nav><!-- .navbar -->

    </div>
</header><!-- End Header -->

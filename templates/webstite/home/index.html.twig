{% extends 'base_website.html.twig' %}

{% block body %}
    {% if about is not null %}
        {{ include('webstite/about/about.html.twig') }}
    {% endif %}
    {#    {{ include('webstite/count/count.html.twig') }}#}

    {#    {{ include('webstite/client/client.html.twig') }}#}
    {% if service is not null %}
        {{ include('webstite/services/service.html.twig') }}
    {% endif %}

    {#    {{ include('webstite/whyus/whyus.html.twig') }}#}
    {% if portfolio is not null %}
        {{ include('webstite/portfolio/portfolio.html.twig') }}
    {% endif %}

    {{ include('webstite/team/team.html.twig') }}

{#    {{ include('webstite/pricing/pricing.html.twig') }}#}

{#    {{ include('webstite/faq/faq.html.twig') }}#}

    {{ include('webstite/contact/contact.html.twig') }}
{% endblock %}
